DECLARE
    res boolean;
BEGIN
    perform public.piggly_branch($PIGGLY$ae44a5a9886714f3$PIGGLY$);
    SELECT x.indisunique
      FROM pg_catalog.pg_index x
      JOIN pg_catalog.pg_class ci ON ci.oid = x.indexrelid
      JOIN pg_catalog.pg_class ct ON ct.oid = x.indrelid
     WHERE ci.relname = $1
       AND pg_catalog.pg_table_is_visible(ct.oid)
      INTO res;

      perform public.piggly_branch($PIGGLY$b32551c1dc73be2d$PIGGLY$);RETURN ok(
          COALESCE(res, false),
          'Index ' || quote_ident($1) || ' should be unique'
      );
END;