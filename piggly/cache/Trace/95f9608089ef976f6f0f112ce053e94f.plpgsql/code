DECLARE
    is_it boolean := _is_instead($1, $2, $3);
BEGIN
    perform public.piggly_branch($PIGGLY$3ffa24784910a227$PIGGLY$);
    IF public.piggly_cond($PIGGLY$b2cf27577e1a850a$PIGGLY$, (is_it IS NOT NULL)) THEN perform public.piggly_branch($PIGGLY$2d9eae63e30df6d0$PIGGLY$);perform public.piggly_branch($PIGGLY$2a1ceffabc612ce7$PIGGLY$);RETURN ok( is_it, $4 ); END IF;
    perform public.piggly_branch($PIGGLY$df77dbfe4e44fb26$PIGGLY$);RETURN ok( FALSE, $4 ) || E'\n' || diag(
        '    Rule ' || quote_ident($3) || ' does not exist'
    );
END;