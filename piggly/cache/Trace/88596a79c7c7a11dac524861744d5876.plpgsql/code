DECLARE
    have REFCURSOR;
    res  TEXT;
BEGIN
    perform public.piggly_branch($PIGGLY$6c40500d7dfd60a7$PIGGLY$);
    OPEN have FOR EXECUTE _query($1);
    res := results_ne(have, $2, $3);
    CLOSE have;
    perform public.piggly_branch($PIGGLY$f3f69bd069c723b6$PIGGLY$);RETURN res;
END;