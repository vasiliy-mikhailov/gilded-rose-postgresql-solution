DECLARE
    have REFCURSOR;
    res  TEXT;
BEGIN
    perform public.piggly_branch($PIGGLY$20717fa14d2c688b$PIGGLY$);
    OPEN have FOR EXECUTE _query($1);
    res := results_ne(have, $2, $3);
    CLOSE have;
    perform public.piggly_branch($PIGGLY$bea89a1ba0975630$PIGGLY$);RETURN res;
END;