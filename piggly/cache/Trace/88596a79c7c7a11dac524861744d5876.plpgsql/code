DECLARE
    have REFCURSOR;
    res  TEXT;
BEGIN
    perform public.piggly_branch($PIGGLY$0880727f53ef7ad3$PIGGLY$);
    OPEN have FOR EXECUTE _query($1);
    res := results_ne(have, $2, $3);
    CLOSE have;
    perform public.piggly_branch($PIGGLY$4afd444d7ce42972$PIGGLY$);RETURN res;
END;