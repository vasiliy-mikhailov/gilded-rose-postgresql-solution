BEGIN
    perform public.piggly_branch($PIGGLY$6f9e1b4c31cc6b7e$PIGGLY$);
    CREATE TEMP TABLE _____coltmp___ AS
    SELECT $1[i]
    FROM generate_series(array_lower($1, 1), array_upper($1, 1)) s(i);
    EXECUTE 'ALTER TABLE _____coltmp___ RENAME TO ' || $2;
    perform public.piggly_branch($PIGGLY$1ea1c213848754f1$PIGGLY$);return $2;
END;