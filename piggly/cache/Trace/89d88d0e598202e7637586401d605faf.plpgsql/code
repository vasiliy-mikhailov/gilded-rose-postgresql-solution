BEGIN
    perform public.piggly_branch($PIGGLY$df64a7f4a294447f$PIGGLY$);
    CREATE TEMP TABLE _____coltmp___ AS
    SELECT $1[i]
    FROM generate_series(array_lower($1, 1), array_upper($1, 1)) s(i);
    EXECUTE 'ALTER TABLE _____coltmp___ RENAME TO ' || $2;
    perform public.piggly_branch($PIGGLY$c6a48c9557fde739$PIGGLY$);return $2;
END;