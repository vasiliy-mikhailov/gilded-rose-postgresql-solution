DECLARE
    want REFCURSOR;
    res  TEXT;
BEGIN
    perform public.piggly_branch($PIGGLY$6eb3117847f0bae9$PIGGLY$);
    OPEN want FOR SELECT $2[i]
    FROM generate_series(array_lower($2, 1), array_upper($2, 1)) s(i);
    res := results_eq($1, want, $3);
    CLOSE want;
    perform public.piggly_branch($PIGGLY$6d3ccb6431b7c830$PIGGLY$);RETURN res;
END;