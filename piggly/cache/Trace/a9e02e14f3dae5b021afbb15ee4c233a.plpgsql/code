DECLARE
    want REFCURSOR;
    res  TEXT;
BEGIN
    perform public.piggly_branch($PIGGLY$ff5850a95f9ac5db$PIGGLY$);
    OPEN want FOR SELECT $2[i]
    FROM generate_series(array_lower($2, 1), array_upper($2, 1)) s(i);
    res := results_eq($1, want, $3);
    CLOSE want;
    perform public.piggly_branch($PIGGLY$0bcec00d180e4725$PIGGLY$);RETURN res;
END;