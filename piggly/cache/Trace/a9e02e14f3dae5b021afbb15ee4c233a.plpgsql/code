DECLARE
    want REFCURSOR;
    res  TEXT;
BEGIN
    perform public.piggly_branch($PIGGLY$dbc88c2dbc394ae0$PIGGLY$);
    OPEN want FOR SELECT $2[i]
    FROM generate_series(array_lower($2, 1), array_upper($2, 1)) s(i);
    res := results_eq($1, want, $3);
    CLOSE want;
    perform public.piggly_branch($PIGGLY$f52ee1379a30d7a4$PIGGLY$);RETURN res;
END;