DECLARE
    have REFCURSOR;
    res  TEXT;
BEGIN
    perform public.piggly_branch($PIGGLY$071e2ff7441c8016$PIGGLY$);
    OPEN have FOR EXECUTE _query($1);
    res := results_eq(have, $2, $3);
    CLOSE have;
    perform public.piggly_branch($PIGGLY$fa3afd9a245c6e55$PIGGLY$);RETURN res;
END;