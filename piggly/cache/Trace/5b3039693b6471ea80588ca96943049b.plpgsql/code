DECLARE
    have REFCURSOR;
    res  TEXT;
BEGIN
    perform public.piggly_branch($PIGGLY$14ea1b6bbc33747d$PIGGLY$);
    OPEN have FOR EXECUTE _query($1);
    res := results_eq(have, $2, $3);
    CLOSE have;
    perform public.piggly_branch($PIGGLY$ba64f23dd50b3f7f$PIGGLY$);RETURN res;
END;